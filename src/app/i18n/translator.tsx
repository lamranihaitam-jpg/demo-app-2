// i18n translator was removed per user request.
// This stub will throw if accidentally imported so the developer notices and can remove the import.
export const TranslatorProvider = () => {
  throw new Error('TranslatorProvider was removed. Remove i18n import.');
}
export const useT = () => {
  throw new Error('useT was removed. Remove i18n import.');
}
